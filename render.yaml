services:
  - type: web
    name: callbridge-pro
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      # Uncomment below if using a Render PostgreSQL database
      # - key: DATABASE_URL
      #   fromDatabase:
      #     name: callbridge-db
      #     property: connectionString
